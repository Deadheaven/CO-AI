# Browser-safe Supabase project values. Never add service-role, GitHub, or
# model-provider secrets here: set those with `supabase secrets set` instead.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key
